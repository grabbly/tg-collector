# ArchiveDrop Web Interface Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Storage Directory (same as bot's STORAGE_DIR)
# Must be an absolute path where bot stores messages
STORAGE_DIR=/opt/tg-collector/storage

# PIN Code for web interface authentication
# IMPORTANT: Change this from the default!
# Use a strong PIN (at least 6 digits recommended)
PIN_CODE=your_secure_pin_here

# Flask Secret Key (used for session encryption)
# IMPORTANT: Generate a strong random secret key!
# Example: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Debug Mode (DO NOT enable in production!)
# Set to 'true' only for development
# Default: false
DEBUG=false

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. PIN_CODE: Should be changed from default and not be '1234'
#    - Recommended: 6-8 digit random PIN
#    - Example: 847293
#
# 2. SECRET_KEY: Must be a long random string
#    - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
#    - Example: a1b2c3d4e5f6...
#
# 3. Never commit this file with real values to git!
#    - The .env file is already in .gitignore
#
# 4. Rate Limiting: Built-in protection against brute-force PIN attempts
#    - 3 failed attempts = 5 minute lockout per IP
#
# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Development Example:
# STORAGE_DIR=./storage
# PIN_CODE=123456
# SECRET_KEY=dev-secret-key-not-for-production
# DEBUG=true

# Production Example:
# STORAGE_DIR=/var/lib/archive-drop/storage
# PIN_CODE=847293
# SECRET_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
# DEBUG=false
